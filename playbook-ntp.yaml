
- name: Déploiement NTP
  hosts: all
  gather_facts: no

  tasks:
    - name: Configurer NTP
      cisco.ios.ios_ntp_global:
        config:
          servers:
          - server: 0.fr.pool.ntp.org"
      
